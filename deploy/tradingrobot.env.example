# Скопируйте в /etc/tradingrobot.env на сервере (chmod 600, владелец root).
# Здесь — реальные секреты. В config.yaml — только имена этих переменных.
#
# TINVEST_TOKEN=...
# TINVEST_ACCOUNT_ID=...
# LLM_API_KEY=...
